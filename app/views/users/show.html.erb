<div class="back-close-nav">
 <%= link_to :back do %>
 <img src="<%= image_path('back-arrow.svg') %>" alt="" style="width:25px; margin:25px"></div>
</div>
<% end %>

<div class="container">
  <div class="user-information" style="display: flex: flex-wrap: wrap;"">
    <h1><%= current_user.name %>'s profil</h1>
      <div>
        <img src="<%= image_path('adam.jpg') %>" alt="" style="width:80px; margin:25px; border-radius: 80px;">"
        <p> edit profil</p>
      </div>
  </div>
<img src="" alt="">

<div class=your-baby-information>




</div>
<% if @babies.any?  %>
  <% @babies.each do |b|  %>

<button class="accordion"><h2><%=b.name%></h2></button>
  <div class="panel">
    <div><p>Birthdate:</p></div>
    <div><p> <%= b.birthdate %></p></div>
    <div> <p> Shared access with:</p> </div>
    <% b.care_takings.each do |c|%>
      <%= c.email %>
      <%= c.name %>
      <%= link_to care_taking_path(c), method: :delete do %>
        <img src="<%= image_path('close.svg') %>" alt="" style="width:10px">
      <% end %>
    <% end %>

      <%= simple_form_for [b, CareTaking.new], method: :post, url: baby_care_takings_path(b) do |f| %>
        <%= f.input(:email)%>
        <%= f.submit("Share Access", class: "button-yellow-small") %>
      <% end %>
  </div>
  <% end %>
<% end %>

</div>

<script>
  var acc = document.getElementsByClassName("accordion");
  var i;

  for (i = 0; i < acc.length; i++) {
    acc[i].addEventListener("click", function() {
      this.classList.toggle("active");
      var panel = this.nextElementSibling;
      if (panel.style.maxHeight) {
        panel.style.maxHeight = null;
      } else {
        panel.style.maxHeight = panel.scrollHeight + "px";
      }
    });
  }
</script>



<!-- Once access shared, + cleared field have a flash message saying "YOU HAVE SHARED ACCESS WITH EMAIL X, your baby's prpofil is now shared"
 -->
<!-- <h2>-------------  Babies shared with you  -------------</h2>
 -->
<%# if current_user.care_takings.count > 0 %>
<%# current_user.care_takings.each do |care_taking| %>
    <%#= link_to care_taking.baby.name , baby_path(care_taking.baby) %>

<!-- </div> -->

  <%# end %>
<%# end %>

